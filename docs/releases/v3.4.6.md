# v3.4.6 Release Notes

GitHub Release: https://github.com/MongLong0214/logic-pro-mcp/releases/tag/v3.4.6
Tag: `v3.4.6`
Release date: 2026-06-09 KST
Source commit: `4592248b9fe70a8d07be0eea800e457c77577d50`
Release workflow: https://github.com/MongLong0214/logic-pro-mcp/actions/runs/27186085967

## Summary

v3.4.6 is the evidence and packaging alignment release after the v3.4.5 Logic Pro 12.2 mixer verification work. It does not add a new Logic runtime contract beyond the v3.4.5 Issues #10-#13 behavior. It published the then-current mainline with the final live E2E cycle-cache timing harness fix, synchronized version surfaces, then-current install docs, then-current Formula SHA, and a fresh stable ADHOC GitHub Release.

## Published Artifacts

| Artifact | SHA256 |
|---|---|
| `LogicProMCP` | `d8d17df6c6f71485c48e53aff91308d36e10d1ba0cdc479c098e0af71b6ad5f3` |
| `LogicProMCP-macOS-universal.tar.gz` | `6420274b4fbbb863226a2a163b971a36161c10880e8f3a06d979100a35bc01d3` |
| `LogicProMCP-macOS-arm64.tar.gz` | `6420274b4fbbb863226a2a163b971a36161c10880e8f3a06d979100a35bc01d3` |

Published `RELEASE-METADATA.json`:

```json
{
  "version": "v3.4.6",
  "team_id": "ADHOC",
  "signing": "adhoc",
  "architectures": ["x86_64", "arm64"]
}
```

## Verification

Local pre-tag and current-main gates:

- `python3 -m py_compile Scripts/live-e2e-test.py` -> PASS.
- `ruby -c Formula/logic-pro-mcp.rb` -> PASS.
- `swift test --filter VersionConsistencyTests --no-parallel` -> 1 / 1 PASS.
- `swift test --enable-code-coverage --no-parallel` -> 1197 / 1197 PASS.
- Coverage TOTAL -> 70.81% region / 78.32% line.
- `swift build -c release` -> PASS.
- `Scripts/release-stable.sh v3.4.6` -> preflight PASS, tag pushed.

Release workflow validation:

- Successful stable workflow run: `27186085967`.
- Jobs: `build`, `validate-install (macos-15)`, and `validate-install (macos-14)` all passed.
- Release state at publication: `draft=false`, `prerelease=false`, stable release. Superseded by published `v3.5.0`; current stable notes are in `docs/releases/v3.7.0.md`.
- Assets uploaded: `LogicProMCP`, `LogicProMCP-macOS-universal.tar.gz`, `LogicProMCP-macOS-arm64.tar.gz`, `RELEASE-METADATA.json`, `SHA256SUMS.txt`.
- Release mode: ADHOC (`team_id:"ADHOC"`, `signing:"adhoc"`), because Developer ID credentials are not configured.

Live Logic Pro evidence:

- Full strict live E2E was completed in the final current-main attestation before this version-only release bump: 285 passed, 0 skipped, 0 failed.
- Targeted Logic Pro 12.2 #10/#11/#12/#13 checks passed in the same final attestation.
- v3.4.6 changed version, installer, Formula, manifest, tests, and docs only; it did not change the runtime mixer implementation verified by that live attestation.

## Known Remaining Scope

- Full per-parameter plugin value readback is not in v3.4.6.
- Arbitrary `set_plugin_param insert:N` targeting is not in v3.4.6.
- Multi-version Logic matrix coverage remains future verification work.
