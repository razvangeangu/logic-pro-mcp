# v3.6.0 Release Notes

Status: stable published release evidence.

GitHub Release: https://github.com/MongLong0214/logic-pro-mcp/releases/tag/v3.6.0
Tag: `v3.6.0`
Release date: 2026-06-19
Source commit: `330a4db1aa8f044fc106f01de6c4ce9e243f7f8d`
Release workflow: [`27803756103`](https://github.com/MongLong0214/logic-pro-mcp/actions/runs/27803756103)

## Summary

v3.6.0 ships the PR #24 verified plugin apply-back surface plus the Logic 12.2 AX readback hardening from PR #54 / issue #59.

The headline change is a new public MCP tool, `logic_plugins`, with three HC v2 commands:

- `get_inventory` reads physical insert-slot inventory from Logic's AX surface.
- `insert_verified` inserts an allowlisted stock plugin into an exact physical insert slot and returns State A only after inventory readback observes the requested plugin at the requested slot.
- `set_param_verified` writes Compressor `threshold` through an open plugin window and returns State A only after AX readback confirms the requested normalized value.

This is a minor release because the public tool surface grows from 8 write tools to 9 and clients can now use a verified stock-plugin apply-back workflow without relying on legacy Scripter send-only parameter writes.

## Added

- New `logic_plugins` tool with HC v2 envelopes (`state`, `hc_schema: 2`, explicit State C `verified:false`).
- Physical insert-slot inventory with slot read status (`empty`, `ok`, `unreadable`) and `complete` gating.
- Exact-slot plugin insertion through the requested slot's own popup, with popup-anchor verification before any plugin leaf is clicked.
- Live-verified insert allowlist for `Gain`, `Channel EQ`, and `Compressor`.
- Live-verified Compressor `threshold` parameter write/readback using normalized `0..100` AX slider values.
- Canonical verified-plugin identity resolution through `VerifiedPluginCatalog`.

## Changed

- `logic_mixer.insert_plugin` remains available for backward compatibility, but new apply-back workflows should use `logic_plugins.insert_verified`.
- `logic_mixer.set_plugin_param` is documented as legacy unverified State B because Scripter can send values but cannot read them back.
- Documentation and packaging now point at the published `v3.6.0` stable release line; `v3.5.0` remains available as the previous stable tag.

## Fixed

- `logic://tracks` no longer falls into placeholder mode on Logic Pro 12.2 / macOS 26 when Logic exposes the track-header rail through language-neutral AX structure instead of stable localized labels.
- Track type inference now scans header descendants across AX description/title/identifier/help/value fields.
- Transport cycle readback now uses the same Logic 12 control-bar path as verified toggle operations.
- Homebrew helper asset install paths remain guarded against formula/tarball layout drift.

## Release-Tree Verification Evidence

Current release-tree evidence:

- PR #24 exact-slot plugin branch: focused Swift suites `63/63`, full `swift test --no-parallel` `1384/1384`, release build, Python live-E2E syntax, stale Search-and-Add grep `0`, targeted live exact-slot insert proof, cleanup inventory readback, and strict live E2E `314/314` passed.
- PR #54 / issue #59 readback hardening: focused AX tests passed, `swift build -c release` passed, full `swift test --no-parallel` `1388/1388` passed, live `logic://tracks` probe returned `source:"ax_live"` with real names, `placeholder_count:0`, `unknown_type_count:0`, and manual cycle toggle/resource roundtrip reflected live UI state.
- v3.6.0 release tree: `git diff --check`, `ruby -c Formula/logic-pro-mcp.rb`, `python3 -m py_compile Scripts/live-e2e-test.py`, `swift test --no-parallel` (`1396/1396`), `swift build -c release`, and strict live Logic Pro 12.2 E2E (`314/314`) passed.

Full release evidence lives in `docs/live-verify-v3.6.0.md`.

## Release Checklist

- [x] Merge PR #24.
- [x] Ensure the PR #54 / issue #59 readback hardening is present in the release branch.
- [x] Bump version surfaces together: `ServerConfig`, `manifest.json`, `Scripts/install.sh`, `Formula/logic-pro-mcp.rb`, version consistency tests, README, SETUP, CHANGELOG, and release evidence docs.
- [x] Run `git diff --check`.
- [x] Run full deterministic tests and release build.
- [x] Run strict live Logic Pro 12.2 E2E on the release tree.
- [x] Run `Scripts/release-stable.sh v3.6.0`.
- [x] Verify the GitHub Release workflow publishes ADHOC universal artifacts and macOS 14/15 install validation passes.
- [x] Copy the published universal tarball SHA256 from `SHA256SUMS.txt` into the Formula as a post-release commit if needed.

## Published Artifacts

GitHub Release workflow `27803756103` completed successfully:

- `build`: universal binary build, ADHOC codesign verification, packaging, Formula/tarball layout gate, and GitHub Release creation passed.
- `validate-install (macos-14)`: pinned installer and uninstaller validation passed.
- `validate-install (macos-15)`: pinned installer and uninstaller validation passed.

`RELEASE-METADATA.json`:

```json
{"version":"v3.6.0","team_id":"ADHOC","signing":"adhoc","architectures":["x86_64","arm64"]}
```

Published asset SHA256 values:

| Asset | SHA256 |
|---|---|
| `LogicProMCP` | `abf2979713087944f1e39fa804a0632940fc07ca67f04316af8dc8265cabb811` |
| `LogicProMCP-macOS-universal.tar.gz` | `28c7a75f1535564ada3c696a42104e52f8654f88564f47bfd673cb0ade26ff83` |
| `LogicProMCP-macOS-arm64.tar.gz` | `28c7a75f1535564ada3c696a42104e52f8654f88564f47bfd673cb0ade26ff83` |
| `SHA256SUMS.txt` | `85e3a98e8744f709faa8854cf011c10be69db500024b5701174ce88fe33b4988` |
| `RELEASE-METADATA.json` | `f898eabdf63ba58892ee598155a0221a408c0e105e135ab877ddc130af0aef80` |

Local post-publish verification downloaded the release assets, matched `SHA256SUMS.txt` against `shasum -a 256`, confirmed `LogicProMCP` is a universal `x86_64 arm64` Mach-O binary, and verified `codesign --verify --strict`.

## Migration Notes

- Clients using only the v3.5.0 read resources do not need to change.
- Clients doing plugin apply-back should migrate from `logic_mixer.set_plugin_param` and `logic_mixer.insert_plugin` to `logic_plugins.get_inventory`, `logic_plugins.insert_verified`, and `logic_plugins.set_param_verified`.
- All mutating `logic_plugins` calls require `mode:"duplicate_applyback"` and `project_expected_path`.
- `set_param_verified` currently supports only Compressor `threshold`. Unsupported params fail closed before writing.
