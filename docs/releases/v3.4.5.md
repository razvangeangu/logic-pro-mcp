# v3.4.5 Release Notes

GitHub Release: https://github.com/MongLong0214/logic-pro-mcp/releases/tag/v3.4.5
Tag: `v3.4.5`
Release date: 2026-06-09 KST
Source commit: `22c7d39fb4f5eadd5c9b6dc6518831f45bbdebd7`
Release workflow: https://github.com/MongLong0214/logic-pro-mcp/actions/runs/27183025739

## Summary

v3.4.5 finalizes and publishes the Logic Pro 12.2 mixer verification work from Issues #10-#13. Stable GitHub Release artifacts are published through the ADHOC path because Apple Developer ID credentials are not configured for this repository. The release still keeps SHA256, codesign verification, metadata, and macOS 14/15 installer validation gates.

- #10: host-originated fader writes can verify through independent AX readback when Logic does not emit an MCU echo.
- #11: `logic://mixer` carries provenance and can refresh from AX poll on Logic 12.2 instead of staying stuck on the connect-time MCU value.
- #12: mixer strips expose AX-sourced plugin slot snapshots with `plugins_source:"ax"`; Scripter plugin writes are Honest-Contract-shaped and fail closed on invalid values or unverified track selection.
- #13: `insert_plugin` is restored only as a guarded path: Level-2 confirmation, stock allowlist, occupied-slot refusal, and AX slot readback.

## Published Artifacts

| Artifact | SHA256 |
|---|---|
| `LogicProMCP` | `66b516c9ad88a832a87a3af21785e76110da8ceeadebeb15fdce025b86cccf1c` |
| `LogicProMCP-macOS-universal.tar.gz` | `4f7a7bda4bbd1fee5a723807e58a460018d2677ae756059eed29590e0b3fe13d` |
| `LogicProMCP-macOS-arm64.tar.gz` | `4f7a7bda4bbd1fee5a723807e58a460018d2677ae756059eed29590e0b3fe13d` |

Published `RELEASE-METADATA.json`:

```json
{
  "version": "v3.4.5",
  "team_id": "ADHOC",
  "signing": "adhoc",
  "architectures": ["x86_64", "arm64"]
}
```

## Verification

Local pre-tag and current-main gates:

- `swift test --no-parallel` -> 1197 / 1197 PASS.
- `swift build -c release` -> PASS.
- `python3 -m py_compile Scripts/live-e2e-test.py` -> PASS.
- `swift test --enable-code-coverage --no-parallel` -> 1197 / 1197 PASS.
- Coverage TOTAL -> 70.40% region / 77.78% line.
- Strict live E2E -> 285 / 285 PASS, 0 skipped.
- Targeted live Logic Pro 12.2 issue checks -> #10/#11/#12/#13 PASS.

Release/CI guard updates included in the stable tag:

- Stable tags are guarded by `Scripts/release-stable.sh` before tag push.
- Stable tags can publish through the ADHOC path when Developer ID credentials are absent.
- CI coverage uses a writable temp `LLVM_PROFILE_FILE` fallback, surfaces LLVM profile runtime warnings, and fail-closes on the generated profdata/report plus region >=70% / line >=77% thresholds while reporting the line >=90% target.
- Hyphenated tags are now published as GitHub prereleases in the release workflow.

GitHub release workflow validation:

- Successful stable workflow run: `27183025739`.
- Jobs: `build`, `validate-install (macos-15)`, and `validate-install (macos-14)` all passed.
- Release state: `draft=false`, `prerelease=false`, latest stable release.
- Historical failed run `27178878939` was blocked under the previous notarization-only policy before artifact publication; it is superseded by the successful ADHOC stable workflow above.
- Recurrence guard: future stable tags must be created through `Scripts/release-stable.sh`, which refuses to create/push the tag until clean `main`, absent duplicate tag/release, and local deterministic gates pass.

## Known Remaining Scope

- Full per-parameter plugin value readback is not in v3.4.5.
- Arbitrary `set_plugin_param insert:N` targeting is not in v3.4.5.
- Full destructive 200+ live E2E and multi-version Logic matrix are separate verification work.
